// * only export is used to export multiple functions
export function addTwoNumbers(a, b) {
  return a + b;
}


// * when u have single function to export from the file 
// * you use "export default"
export default function renderHello() {
  return <p>Hello</p>;
}
